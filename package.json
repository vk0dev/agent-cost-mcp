{
  "name": "@vk0/agent-cost-mcp",
  "version": "0.1.0",
  "description": "Lightweight token and cost tracking for Claude Code agents. Parses session logs to surface per-tool spending and optimization hints.",
  "mcpName": "io.github.vk0dev/agent-cost-mcp",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "bin": {
    "agent-cost-mcp": "./dist/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "mcp",
    "mcp-server",
    "claude-code",
    "ai-agent",
    "cost-tracking",
    "tokens"
  ],
  "author": "vk0.dev",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vk0dev/agent-cost-mcp.git"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
