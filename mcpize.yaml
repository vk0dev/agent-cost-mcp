# MCPize manifest - https://mcpize.com
# Generated by mcpize analyze
version: 1
name: '@vk0/agent-cost-mcp'
description: >-
  Local-first Claude Code cost analyzer. Parses JSONL session logs to surface per-tool spend, daily trends, and
  optimization hints. Zero cloud, no API key.
runtime: typescript
entry: dist/server.js
build:
  install: npm ci
  command: npm run build
startCommand:
  type: stdio
  command: node dist/server.js
